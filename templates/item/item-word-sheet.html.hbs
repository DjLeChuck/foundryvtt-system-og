<form class="{{cssClass}}" autocomplete="off">
  <header class="sheet-header">
    <div class="header-fields">
      <img class="profile-img" src="{{item.img}}" data-edit="img" title="{{item.name}}" />
      <h1 class="charname">
        <input name="name" type="text" value="{{item.name}}" placeholder="{{localize "OG.Global.Name"}}" /></h1>
    </div>
  </header>

  <section class="sheet-body">
    <label for="item-common-{{item._id}}">{{localize "OG.Word.IsCommon"}}</label>
    <input name="system.common" type="checkbox" {{checked item.system.common}}
           placeholder="{{localize "OG.Word.IsCommon"}}" id="item-common-{{item._id}}" />
  </section>
</form>
